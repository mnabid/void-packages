site="https://github.com/vinceliuice/Colloid-gtk-theme/commits/main.atom"
pattern='[\d.]+'

curl() {
	if [[ "$*" == *"/commits/"* ]]; then
		command curl "$@" | grep -Po '<updated>\K[0-9]{4}-[0-9]{2}-[0-9]{2}' | tr -d '-' | sed 's/^/0.0./'
	else
		command curl "$@"
	fi
}
